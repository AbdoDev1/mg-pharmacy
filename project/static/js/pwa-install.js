/*
 * سكريبت واحد مشترك بين الصفحة الرئيسية (templates/landing.html) وقاعدة
 * العميل/المتجر (templates/base.html) وقاعدة لوحة الموظفين
 * (staff/templates/staff/base.html)، عشان يشتغل بنفس المنطق بالظبط في كل
 * الموقع من غير تكرار كود. مسؤوليته الوحيدة دلوقتي: تسجيل الـ service
 * worker — شرط أساسي عشان المتصفح (كروم/إدج) يقدر يعتبر الموقع "قابل
 * للتثبيت" ويظهر أيقونة التثبيت بتاعته هو (في شريط العنوان، زي يوتيوب
 * بالظبط) تلقائيًا من غير أي تدخل زيادة منّا.
 *
 * ملحوظتين على حاجتين اتشالوا عمدًا واتجرّبوا وترفضوا:
 *   - زرار "تثبيت التطبيق" المخصص (كان بينادي preventDefault على
 *     beforeinstallprompt) — ده كان بالظبط اللي بيخفي أيقونة كروم
 *     الأصلية بدل ما يضيف ليها حاجة، فاتشال بالكامل.
 *   - التوب بار البديل (رجوع/تقدّم/تحديث) اللي كان بيظهر لما التطبيق
 *     شغال standalone (مثبّت) — اتشال بالكامل برضه بناءً على طلب
 *     صريح: الشريط الأزرق (الناف بار العادي) كفاية لوحده.
 *
 * محمّل بـ <script defer src> في <head> (مش inline) عمدًا: في لوحة
 * الموظفين اللي بتستخدم hx-boost="true" على <body>، htmx بيدمج <head>
 * بدل ما يستبدله، ولو لاقى نفس src موجود مش بيعيد تحميله/تنفيذه تاني (نفس
 * سبب تحميل Chart.js مركزيًا في staff/base.html) — فالسكريبت ده بيتنفذ
 * مرة واحدة بس طول عمر الصفحة تلقائيًا.
 */
(function () {
    'use strict';

    if ('serviceWorker' in navigator) {
        window.addEventListener('load', function () {
            navigator.serviceWorker.register('/sw.js', { scope: '/' }).catch(function () {
                // فشل التسجيل (مثلاً http محلي بدل https، أو متصفح مقيّد)
                // مش خطأ حرج — الموقع بيشتغل عادي من غيره، بس بدون أيقونة
                // التثبيت التلقائية في المتصفحات اللي محتاجاها.
            });
        });
    }
})();
