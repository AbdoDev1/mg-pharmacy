/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './templates/**/*.html',
    './*/templates/**/*.html',
    // staff_ui.py بيبني كلاسات الشارات/الأزرار كـ strings ثابتة في بايثون، مش
    // في HTML، فلازم يتفحص برضه عشان Tailwind الـ purge ميشيلهاش باعتبارها مش مستخدمة.
    './staff/templatetags/*.py',
  ],
  theme: {
    extend: {
      colors: {
        // لون أساسي واحد موحّد لكل النظام (هيدر لوحة الموظفين، أزرار
        // رئيسية، شارات نشطة، إلخ). غيّر القيم هنا بس، مش في كل template
        // — كل استخدام لـ bg-primary-*/text-primary-*/border-primary-*
        // في أي مكان في المشروع هيتحدّث تلقائيًا بعد أي npm run build:css.
        //
        // Phase 1 (Branding): هوية MG Pharmacy — سلم درجات مبني حوالين
        // لونين أساسيين معتمدين: Navy #071C42 (900) و Dark Navy #0D2B5B
        // (600 — ده اللي كان لازم يبقى بديل الأزرق القديم #1d4ed8 في كل
        // الهيدر/الأزرار). باقي الدرجات (50-500, 700-800) اتحسبت تدريجيًا
        // بينهم عشان يفضل فيه تباين كافي (زي text-primary-100 اللي بيتكتب
        // فوق خلفية غامقة في navbar/bell.html).
        primary: {
          50: '#eef1f6',
          100: '#dbe2ec',
          200: '#b7c5d9',
          300: '#8ea1bf',
          400: '#5c7ba1',
          500: '#375d85',
          600: '#0D2B5B',
          700: '#0a2249',
          800: '#071C42',
          900: '#050f28',
        },
        // لون التمييز (Gold/Light Gold) — مش مستخدم كـ bg-primary-* بديل،
        // متاح كطبقة تانية (bg-gold-*/text-gold-*) للمراحل الجاية (زي أزرار
        // "أضف للسلة"/badges في Phase 5) من غير ما نضطر نرجع نلمس الملف ده.
        gold: {
          400: '#E0B74E',
          500: '#C99A2E',
          600: '#ab8324',
        },
        // خلفية الصفحة الجديدة في لوحة الموظفين — أغمق شوية من bg-gray-100
        // القديمة عشان يبان فرق حقيقي بينها وبين الكروت البيضاء فوقها.
        surface: {
          page: '#e7eaf1',
        },
        // Phase 6 (Stitch design import — أغسطس 2026): خلفية "Sterile" الفاتحة
        // من DESIGN.md (#E6F0F8) — مستخدمة في شرائط الفئات/الـ chips وحد
        // الهيدر السفلي بدل الشادو. اسم منفصل عشان منلمسش primary/gold.
        chip: '#E6F0F8',
      },
      boxShadow: {
        // ظل محيطي خفيف جدًا من DESIGN.md لكروت المنتجات/المودالز —
        // بديل الشادوهات الغامقة الافتراضية.
        soft: '0px 4px 20px rgba(10, 29, 68, 0.05)',
      },
      borderRadius: {
        // نصف قطر الحاويات (كروت المنتجات والمحتوى الرئيسي) من DESIGN.md
        card: '1rem',
      },
    },
  },
  plugins: [],
  // مفيش safelist مطلوبة: كل الكلاسات مكتوبة كاملة صراحة في التمبليتس (زي bg-blue-600)
  // من غير تركيب ديناميكي بالـ string concatenation، فالـ content scan بيلقطها كلها.
};
